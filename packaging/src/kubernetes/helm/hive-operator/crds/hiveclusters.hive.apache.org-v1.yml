# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: hiveclusters.hive.apache.org
spec:
  group: hive.apache.org
  names:
    kind: HiveCluster
    plural: hiveclusters
    shortNames:
    - hc
    singular: hivecluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.clusterPhase
      name: Phase
      priority: 0
      type: string
    - jsonPath: .status.idleForMinutes
      name: Idle (min)
      priority: 0
      type: integer
    - jsonPath: .status.suspendedSince
      name: Suspended Since
      priority: 1
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              autoSuspend:
                description: "Auto-suspend configuration. When enabled and all components\
                  \ are idle for the configured timeout, the cluster scales to 0 replicas."
                properties:
                  enabled:
                    default: false
                    description: "Whether auto-suspend is enabled. Requires autoscaling\
                      \ to be enabled on all active components (HS2, LLAP if enabled,\
                      \ TezAM if enabled, and HMS if includeMetastore is true)."
                    type: boolean
                  idleTimeoutMinutes:
                    default: 15
                    description: "Minutes of idle time (HS2=0 sessions, LLAP/TezAM\
                      \ at minReplicas) before the cluster auto-suspends."
                    type: integer
                  includeMetastore:
                    default: true
                    description: "Whether Metastore participates in auto-suspend.\
                      \ When false, HMS stays at minReplicas during suspend and HMS\
                      \ autoscaling is not required for auto-suspend to activate."
                    type: boolean
                type: object
              envVars:
                description: "Environment variables injected into all component pods\
                  \ (e.g., storage credentials, custom JVM options)"
                items:
                  type: object
                type: array
                x-kubernetes-preserve-unknown-fields: true
              externalJars:
                description: "External JARs (URLs) downloaded into all component pods\
                  \ and added to HADOOP_CLASSPATH (e.g., GCS connector, ABFS connector)"
                items:
                  type: string
                type: array
              hadoop:
                description: Hadoop/core-site.xml configuration overrides
                properties:
                  coreSiteOverrides:
                    additionalProperties:
                      type: string
                    description: Key-value pairs written into core-site.xml
                    type: object
                type: object
              hiveServer2:
                description: HiveServer2 component configuration
                properties:
                  autoscaling:
                    description: "Autoscaling configuration (operator-driven, no external\
                      \ dependencies)"
                    properties:
                      cpuScaleDownThreshold:
                        default: 30
                        description: CPU percentage (0-100) below which scale-down
                          is considered. Only applies to HS2 and HMS.
                        type: integer
                      cpuScaleUpThreshold:
                        default: 90
                        description: CPU percentage (0-100) that triggers scale-up.
                          Only applies to HS2 and HMS. Set to 0 to disable CPU-based
                          scaling.
                        type: integer
                      enabled:
                        default: false
                        description: Whether autoscaling is enabled for this component
                        type: boolean
                      gracePeriodSeconds:
                        default: 3600
                        description: Maximum time in seconds to wait for graceful
                          drain during scale-down before the pod is forcibly terminated.
                          The pod terminates immediately once sessions/connections
                          drain to 0; this value is only the upper safety cap.
                        type: integer
                      metricsPort:
                        default: 9404
                        description: Port on which the Prometheus JMX Exporter serves
                          metrics. The operator scrapes this port on each pod for
                          autoscaling decisions.
                        type: integer
                      metricsScrapeIntervalSeconds:
                        default: 10
                        description: How often (seconds) the operator scrapes JMX
                          metrics from pods. Lower values make autoscaling react faster.
                        type: integer
                      minReplicas:
                        default: 0
                        description: "Minimum number of replicas (floor for scale-down).\
                          \ Set to 0 for scale-to-zero (LLAP, TezAM only; HS2 minimum\
                          \ is 1)"
                        type: integer
                      scaleDownStabilizationSeconds:
                        default: 600
                        description: Stabilization window in seconds for scale-down
                          decisions. How long metrics must consistently indicate fewer
                          replicas before scale-down occurs. Also acts as the cooldown
                          between consecutive scale-downs.
                        type: integer
                      scaleUpStabilizationSeconds:
                        default: 60
                        description: Stabilization window in seconds for scale-up
                          decisions. Picks the highest recommendation within this
                          window to prevent flapping.
                        type: integer
                      scaleUpThreshold:
                        default: 80
                        description: "Threshold that triggers scale-up (component-specific:\
                          \ sessions per pod for HS2, request rate for HMS, busy slots\
                          \ per daemon for LLAP). Not used by TezAM (demand-based:\
                          \ 1 TezAM per session)."
                        type: integer
                    type: object
                  configOverrides:
                    additionalProperties:
                      type: string
                    description: Additional configuration overrides as key-value pairs
                    type: object
                  externalJars:
                    description: 'List of URIs to external JARs to download and add
                      to HS2 classpath '
                    items:
                      type: string
                    type: array
                  extraVolumeMounts:
                    description: Additional volume mounts for the container
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: "Additional volumes to attach to the pod (e.g., for\
                      \ keytabs or truststores)"
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  livenessProbe:
                    description: Liveness probe configuration
                    properties:
                      failureThreshold:
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        type: integer
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before probes are initiated.
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                        type: integer
                      successThreshold:
                        description: Minimum consecutive successes for the probe to
                          be considered successful after having failed.
                        type: integer
                      timeoutSeconds:
                        description: Number of seconds after which the probe times
                          out.
                        type: integer
                    type: object
                  readinessProbe:
                    description: Readiness probe configuration
                    properties:
                      failureThreshold:
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        type: integer
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before probes are initiated.
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                        type: integer
                      successThreshold:
                        description: Minimum consecutive successes for the probe to
                          be considered successful after having failed.
                        type: integer
                      timeoutSeconds:
                        description: Number of seconds after which the probe times
                          out.
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    description: Number of replicas
                    type: integer
                  resources:
                    description: Resource requirements for pods
                    properties:
                      limitsCpu:
                        description: "CPU limit (e.g. 2, 1000m)"
                        type: string
                      limitsMemory:
                        description: "Memory limit (e.g. 2Gi, 1024Mi)"
                        type: string
                      requestsCpu:
                        default: 500m
                        description: "CPU request (e.g. 500m, 1)"
                        type: string
                      requestsMemory:
                        default: 1Gi
                        description: "Memory request (e.g. 1Gi, 512Mi)"
                        type: string
                    type: object
                  serviceType:
                    default: ClusterIP
                    description: "Kubernetes Service type: ClusterIP, LoadBalancer,\
                      \ or NodePort"
                    type: string
                type: object
                x-kubernetes-preserve-unknown-fields: true
              image:
                description: Docker image to use for all Hive components
                type: string
              imagePullPolicy:
                description: "Image pull policy: Always, Never, or IfNotPresent"
                type: string
              llap:
                description: LLAP daemon configuration. Enabled by default.
                properties:
                  autoscaling:
                    description: "Autoscaling configuration (operator-driven, no external\
                      \ dependencies)"
                    properties:
                      cpuScaleDownThreshold:
                        default: 30
                        description: CPU percentage (0-100) below which scale-down
                          is considered. Only applies to HS2 and HMS.
                        type: integer
                      cpuScaleUpThreshold:
                        default: 90
                        description: CPU percentage (0-100) that triggers scale-up.
                          Only applies to HS2 and HMS. Set to 0 to disable CPU-based
                          scaling.
                        type: integer
                      enabled:
                        default: false
                        description: Whether autoscaling is enabled for this component
                        type: boolean
                      gracePeriodSeconds:
                        default: 3600
                        description: Maximum time in seconds to wait for graceful
                          drain during scale-down before the pod is forcibly terminated.
                          The pod terminates immediately once sessions/connections
                          drain to 0; this value is only the upper safety cap.
                        type: integer
                      metricsPort:
                        default: 9404
                        description: Port on which the Prometheus JMX Exporter serves
                          metrics. The operator scrapes this port on each pod for
                          autoscaling decisions.
                        type: integer
                      metricsScrapeIntervalSeconds:
                        default: 10
                        description: How often (seconds) the operator scrapes JMX
                          metrics from pods. Lower values make autoscaling react faster.
                        type: integer
                      minReplicas:
                        default: 0
                        description: "Minimum number of replicas (floor for scale-down).\
                          \ Set to 0 for scale-to-zero (LLAP, TezAM only; HS2 minimum\
                          \ is 1)"
                        type: integer
                      scaleDownStabilizationSeconds:
                        default: 600
                        description: Stabilization window in seconds for scale-down
                          decisions. How long metrics must consistently indicate fewer
                          replicas before scale-down occurs. Also acts as the cooldown
                          between consecutive scale-downs.
                        type: integer
                      scaleUpStabilizationSeconds:
                        default: 60
                        description: Stabilization window in seconds for scale-up
                          decisions. Picks the highest recommendation within this
                          window to prevent flapping.
                        type: integer
                      scaleUpThreshold:
                        default: 80
                        description: "Threshold that triggers scale-up (component-specific:\
                          \ sessions per pod for HS2, request rate for HMS, busy slots\
                          \ per daemon for LLAP). Not used by TezAM (demand-based:\
                          \ 1 TezAM per session)."
                        type: integer
                    type: object
                  configOverrides:
                    additionalProperties:
                      type: string
                    description: Additional configuration overrides as key-value pairs
                    type: object
                  enabled:
                    default: true
                    description: Whether LLAP is enabled
                    type: boolean
                  executors:
                    default: 1
                    description: Number of LLAP executors per daemon
                    type: integer
                  extraVolumeMounts:
                    description: Additional volume mounts for the container
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: "Additional volumes to attach to the pod (e.g., for\
                      \ keytabs or truststores)"
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  memoryMb:
                    default: 1024
                    description: Memory in MB per LLAP daemon instance
                    type: integer
                  readinessProbe:
                    description: Readiness probe configuration
                    properties:
                      failureThreshold:
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        type: integer
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before probes are initiated.
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                        type: integer
                      successThreshold:
                        description: Minimum consecutive successes for the probe to
                          be considered successful after having failed.
                        type: integer
                      timeoutSeconds:
                        description: Number of seconds after which the probe times
                          out.
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    description: Number of replicas
                    type: integer
                  resources:
                    description: Resource requirements for pods
                    properties:
                      limitsCpu:
                        description: "CPU limit (e.g. 2, 1000m)"
                        type: string
                      limitsMemory:
                        description: "Memory limit (e.g. 2Gi, 1024Mi)"
                        type: string
                      requestsCpu:
                        default: 500m
                        description: "CPU request (e.g. 500m, 1)"
                        type: string
                      requestsMemory:
                        default: 1Gi
                        description: "Memory request (e.g. 1Gi, 512Mi)"
                        type: string
                    type: object
                  serviceHosts:
                    description: LLAP service hosts identifier for ZooKeeper registration
                    type: string
                type: object
                x-kubernetes-preserve-unknown-fields: true
              metastore:
                description: Metastore component configuration
                properties:
                  autoscaling:
                    description: "Autoscaling configuration (operator-driven, no external\
                      \ dependencies)"
                    properties:
                      cpuScaleDownThreshold:
                        default: 30
                        description: CPU percentage (0-100) below which scale-down
                          is considered. Only applies to HS2 and HMS.
                        type: integer
                      cpuScaleUpThreshold:
                        default: 90
                        description: CPU percentage (0-100) that triggers scale-up.
                          Only applies to HS2 and HMS. Set to 0 to disable CPU-based
                          scaling.
                        type: integer
                      enabled:
                        default: false
                        description: Whether autoscaling is enabled for this component
                        type: boolean
                      gracePeriodSeconds:
                        default: 3600
                        description: Maximum time in seconds to wait for graceful
                          drain during scale-down before the pod is forcibly terminated.
                          The pod terminates immediately once sessions/connections
                          drain to 0; this value is only the upper safety cap.
                        type: integer
                      metricsPort:
                        default: 9404
                        description: Port on which the Prometheus JMX Exporter serves
                          metrics. The operator scrapes this port on each pod for
                          autoscaling decisions.
                        type: integer
                      metricsScrapeIntervalSeconds:
                        default: 10
                        description: How often (seconds) the operator scrapes JMX
                          metrics from pods. Lower values make autoscaling react faster.
                        type: integer
                      minReplicas:
                        default: 0
                        description: "Minimum number of replicas (floor for scale-down).\
                          \ Set to 0 for scale-to-zero (LLAP, TezAM only; HS2 minimum\
                          \ is 1)"
                        type: integer
                      scaleDownStabilizationSeconds:
                        default: 600
                        description: Stabilization window in seconds for scale-down
                          decisions. How long metrics must consistently indicate fewer
                          replicas before scale-down occurs. Also acts as the cooldown
                          between consecutive scale-downs.
                        type: integer
                      scaleUpStabilizationSeconds:
                        default: 60
                        description: Stabilization window in seconds for scale-up
                          decisions. Picks the highest recommendation within this
                          window to prevent flapping.
                        type: integer
                      scaleUpThreshold:
                        default: 80
                        description: "Threshold that triggers scale-up (component-specific:\
                          \ sessions per pod for HS2, request rate for HMS, busy slots\
                          \ per daemon for LLAP). Not used by TezAM (demand-based:\
                          \ 1 TezAM per session)."
                        type: integer
                    type: object
                  configOverrides:
                    additionalProperties:
                      type: string
                    description: Additional configuration overrides as key-value pairs
                    type: object
                  database:
                    description: Database connection configuration for the metastore
                      backend
                    properties:
                      driver:
                        description: JDBC driver class name
                        type: string
                      driverJarUrl:
                        description: "URL to download the JDBC driver jar, e.g. https://repo1.maven.org/maven2/org/postgresql/postgresql/42.7.5/postgresql-42.7.5.jar"
                        type: string
                      passwordSecretRef:
                        description: Reference to a Kubernetes Secret containing the
                          database password
                        properties:
                          key:
                            description: Key within the Secret
                            type: string
                          name:
                            description: Name of the Kubernetes Secret
                            type: string
                        type: object
                      type:
                        default: derby
                        description: "Database type: derby, mysql, postgres, mssql,\
                          \ or oracle"
                        type: string
                      url:
                        description: JDBC connection URL
                        type: string
                      username:
                        description: Database username
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: Whether the operator should deploy and manage a Metastore
                    type: boolean
                  externalUri:
                    description: Thrift URI of the external Metastore (if enabled
                      is false)
                    type: string
                  extraVolumeMounts:
                    description: Additional volume mounts for the container
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: "Additional volumes to attach to the pod (e.g., for\
                      \ keytabs or truststores)"
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  livenessProbe:
                    description: Liveness probe configuration
                    properties:
                      failureThreshold:
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        type: integer
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before probes are initiated.
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                        type: integer
                      successThreshold:
                        description: Minimum consecutive successes for the probe to
                          be considered successful after having failed.
                        type: integer
                      timeoutSeconds:
                        description: Number of seconds after which the probe times
                          out.
                        type: integer
                    type: object
                  readinessProbe:
                    description: Readiness probe configuration
                    properties:
                      failureThreshold:
                        description: Minimum consecutive failures for the probe to
                          be considered failed after having succeeded.
                        type: integer
                      initialDelaySeconds:
                        description: Number of seconds after the container has started
                          before probes are initiated.
                        type: integer
                      periodSeconds:
                        description: How often (in seconds) to perform the probe.
                        type: integer
                      successThreshold:
                        description: Minimum consecutive successes for the probe to
                          be considered successful after having failed.
                        type: integer
                      timeoutSeconds:
                        description: Number of seconds after which the probe times
                          out.
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    description: Number of replicas
                    type: integer
                  resources:
                    description: Resource requirements for pods
                    properties:
                      limitsCpu:
                        description: "CPU limit (e.g. 2, 1000m)"
                        type: string
                      limitsMemory:
                        description: "Memory limit (e.g. 2Gi, 1024Mi)"
                        type: string
                      requestsCpu:
                        default: 500m
                        description: "CPU request (e.g. 500m, 1)"
                        type: string
                      requestsMemory:
                        default: 1Gi
                        description: "Memory request (e.g. 1Gi, 512Mi)"
                        type: string
                    type: object
                  warehouseDir:
                    default: /hive/warehouse
                    description: Warehouse directory path
                    type: string
                type: object
                x-kubernetes-preserve-unknown-fields: true
              suspend:
                description: "When true, the cluster is immediately suspended (all\
                  \ components scaled to 0). Set to false to wake a suspended cluster."
                type: boolean
              tezAm:
                description: Tez Application Master configuration. Enabled by default.
                properties:
                  autoscaling:
                    description: "Autoscaling configuration (operator-driven, no external\
                      \ dependencies)"
                    properties:
                      cpuScaleDownThreshold:
                        default: 30
                        description: CPU percentage (0-100) below which scale-down
                          is considered. Only applies to HS2 and HMS.
                        type: integer
                      cpuScaleUpThreshold:
                        default: 90
                        description: CPU percentage (0-100) that triggers scale-up.
                          Only applies to HS2 and HMS. Set to 0 to disable CPU-based
                          scaling.
                        type: integer
                      enabled:
                        default: false
                        description: Whether autoscaling is enabled for this component
                        type: boolean
                      gracePeriodSeconds:
                        default: 3600
                        description: Maximum time in seconds to wait for graceful
                          drain during scale-down before the pod is forcibly terminated.
                          The pod terminates immediately once sessions/connections
                          drain to 0; this value is only the upper safety cap.
                        type: integer
                      metricsPort:
                        default: 9404
                        description: Port on which the Prometheus JMX Exporter serves
                          metrics. The operator scrapes this port on each pod for
                          autoscaling decisions.
                        type: integer
                      metricsScrapeIntervalSeconds:
                        default: 10
                        description: How often (seconds) the operator scrapes JMX
                          metrics from pods. Lower values make autoscaling react faster.
                        type: integer
                      minReplicas:
                        default: 0
                        description: "Minimum number of replicas (floor for scale-down).\
                          \ Set to 0 for scale-to-zero (LLAP, TezAM only; HS2 minimum\
                          \ is 1)"
                        type: integer
                      scaleDownStabilizationSeconds:
                        default: 600
                        description: Stabilization window in seconds for scale-down
                          decisions. How long metrics must consistently indicate fewer
                          replicas before scale-down occurs. Also acts as the cooldown
                          between consecutive scale-downs.
                        type: integer
                      scaleUpStabilizationSeconds:
                        default: 60
                        description: Stabilization window in seconds for scale-up
                          decisions. Picks the highest recommendation within this
                          window to prevent flapping.
                        type: integer
                      scaleUpThreshold:
                        default: 80
                        description: "Threshold that triggers scale-up (component-specific:\
                          \ sessions per pod for HS2, request rate for HMS, busy slots\
                          \ per daemon for LLAP). Not used by TezAM (demand-based:\
                          \ 1 TezAM per session)."
                        type: integer
                    type: object
                  configOverrides:
                    additionalProperties:
                      type: string
                    description: Additional configuration overrides as key-value pairs
                    type: object
                  enabled:
                    default: true
                    description: Whether Tez AM is enabled
                    type: boolean
                  extraVolumeMounts:
                    description: Additional volume mounts for the container
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: "Additional volumes to attach to the pod (e.g., for\
                      \ keytabs or truststores)"
                    items:
                      type: object
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  replicas:
                    default: 1
                    description: Number of replicas
                    type: integer
                  resources:
                    description: Resource requirements for pods
                    properties:
                      limitsCpu:
                        description: "CPU limit (e.g. 2, 1000m)"
                        type: string
                      limitsMemory:
                        description: "Memory limit (e.g. 2Gi, 1024Mi)"
                        type: string
                      requestsCpu:
                        default: 500m
                        description: "CPU request (e.g. 500m, 1)"
                        type: string
                      requestsMemory:
                        default: 1Gi
                        description: "Memory request (e.g. 1Gi, 512Mi)"
                        type: string
                    type: object
                  scratchStorageClassName:
                    description: "StorageClass for the shared scratch PVC. Must support\
                      \ ReadWriteMany access. If null, uses cluster default."
                    type: string
                  scratchStorageSize:
                    default: 1Gi
                    description: Storage size for the shared scratch PVC (ReadWriteMany)
                      mounted on HS2 and TezAM at /opt/hive/scratch
                    type: string
                type: object
                x-kubernetes-preserve-unknown-fields: true
              volumeMounts:
                description: "Volume mounts added to all component containers (e.g.,\
                  \ mounting a GCS key file at /etc/gcs/key.json)"
                items:
                  type: object
                type: array
                x-kubernetes-preserve-unknown-fields: true
              volumes:
                description: "Volumes added to all component pods (e.g., Secrets containing\
                  \ keytabs or service account keys)"
                items:
                  type: object
                type: array
                x-kubernetes-preserve-unknown-fields: true
              zookeeper:
                description: External ZooKeeper connection details (not managed by
                  this operator)
                properties:
                  quorum:
                    description: ZooKeeper quorum connection string. This field is
                      strictly required.
                    type: string
                required:
                - quorum
                type: object
            required:
            - zookeeper
            type: object
            x-kubernetes-preserve-unknown-fields: true
          status:
            properties:
              clusterPhase:
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              hiveServer2:
                properties:
                  autoscaling:
                    properties:
                      cpuProposedReplicas:
                        type: integer
                      cpuScaleUpThreshold:
                        type: integer
                      currentCpuPercent:
                        type: number
                      currentMetricValue:
                        type: integer
                      lastScaleTime:
                        type: string
                      proposedReplicas:
                        type: integer
                      scaleUpThreshold:
                        type: integer
                    type: object
                  currentReplicas:
                    type: integer
                  maxReplicas:
                    type: integer
                  minReplicas:
                    type: integer
                  phase:
                    type: string
                  readyReplicas:
                    type: integer
                type: object
              idleForMinutes:
                type: integer
              idleSince:
                type: string
              llap:
                properties:
                  autoscaling:
                    properties:
                      cpuProposedReplicas:
                        type: integer
                      cpuScaleUpThreshold:
                        type: integer
                      currentCpuPercent:
                        type: number
                      currentMetricValue:
                        type: integer
                      lastScaleTime:
                        type: string
                      proposedReplicas:
                        type: integer
                      scaleUpThreshold:
                        type: integer
                    type: object
                  currentReplicas:
                    type: integer
                  maxReplicas:
                    type: integer
                  minReplicas:
                    type: integer
                  phase:
                    type: string
                  readyReplicas:
                    type: integer
                type: object
              metastore:
                properties:
                  autoscaling:
                    properties:
                      cpuProposedReplicas:
                        type: integer
                      cpuScaleUpThreshold:
                        type: integer
                      currentCpuPercent:
                        type: number
                      currentMetricValue:
                        type: integer
                      lastScaleTime:
                        type: string
                      proposedReplicas:
                        type: integer
                      scaleUpThreshold:
                        type: integer
                    type: object
                  currentReplicas:
                    type: integer
                  maxReplicas:
                    type: integer
                  minReplicas:
                    type: integer
                  phase:
                    type: string
                  readyReplicas:
                    type: integer
                type: object
              observedGeneration:
                type: integer
              suspendedSince:
                type: string
              tezAm:
                properties:
                  autoscaling:
                    properties:
                      cpuProposedReplicas:
                        type: integer
                      cpuScaleUpThreshold:
                        type: integer
                      currentCpuPercent:
                        type: number
                      currentMetricValue:
                        type: integer
                      lastScaleTime:
                        type: string
                      proposedReplicas:
                        type: integer
                      scaleUpThreshold:
                        type: integer
                    type: object
                  currentReplicas:
                    type: integer
                  maxReplicas:
                    type: integer
                  minReplicas:
                    type: integer
                  phase:
                    type: string
                  readyReplicas:
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
